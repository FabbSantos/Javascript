// variaveis booleanas só podem ter 2 valores, true ou false.
// são importantes na programação, normalmente são usadas para validação ou definição de fluxo de código.

let sim = true;
let nao = false;

let resultado = 10 > 5;

console.log(sim, nao, resultado)