// o valor especial NULL não pertence a nenhum outro tipo de dado. é um tipo próprio que só pode ter esse valor;

let nulo = null;

// é a mesma coisa que nada, vazio, desconhecido ou não definido.

let idade = null;
console.log(nulo, idade);